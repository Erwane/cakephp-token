parameters:
    git_dir: .
    bin_dir: vendor/bin
    ascii:
        failed: ~
        succeeded: ~
    tasks:
        phplint: ~
        phpcs:
            report: full
            standard: CakePHP
            encoding: UTF-8
            whitelist_patterns:
                - /^src\/(.*\.php)/
